name: pdf_printer
description: A web server built using the shelf package.
# version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  # flutter:
  #   sdk: flutter
  args: ^2.0.0
  pdf: ^3.0.1
  shelf: ^1.1.0

dev_dependencies:
  pedantic: ^1.11.0
  build_runner: 
  build_web_compilers: